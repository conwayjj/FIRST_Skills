**Card** — clean white content surface with a soft neutral shadow. Optional Team Blue top accent rule; set `interactive` for hover lift.

```jsx
<Card accent interactive>
  <h3>FIRST® Robotics Competition</h3>
  <p>Grades 9-12 build an industrial-sized robot…</p>
</Card>
```

Props: `accent` (blue top rule), `padding` (`sm`/`md`/`lg`), `interactive` (hover lift).
